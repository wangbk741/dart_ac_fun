import 'package:flutter/material.dart';
import 'VideoPart/Video_Player/FIJKPlayer.dart';
class ArticleTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: FlatButton(
         onPressed: () {

                  
        }, child: Text('data'),
        ),
      )
    );
  }

}